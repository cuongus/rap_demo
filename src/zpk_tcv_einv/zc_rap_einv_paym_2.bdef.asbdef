projection;
strict ( 2 );
use draft;

define behavior for ZC_RAP_EINV_PAYM_2 alias EInvoicePayment
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}